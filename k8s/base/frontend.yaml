apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: greenpay
  labels:
    app: frontend
    color: blue
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
      color: blue
  template:
    metadata:
      labels:
        app: frontend
        color: blue
    spec:
      containers:
        - name: frontend
          image: greenpay/frontend:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: greenpay-config
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 10
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
# Stable Service: see backend.yaml for how selector.color is used as the
# blue/green cutover switch by scripts/blue-green-deploy.sh.
apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  namespace: greenpay
spec:
  selector:
    app: frontend
    color: blue
  ports:
    - port: 3000
      targetPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-svc-blue
  namespace: greenpay
spec:
  selector:
    app: frontend
    color: blue
  ports:
    - port: 3000
      targetPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-svc-green
  namespace: greenpay
spec:
  selector:
    app: frontend
    color: green
  ports:
    - port: 3000
      targetPort: 3000
